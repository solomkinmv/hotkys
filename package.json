{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "shortcuts",
  "title": "Shortcuts",
  "description": "",
  "icon": "command-icon.png",
  "author": "tiny.water2342",
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Show shortcuts for the frontmost app base",
      "description": "Show shortcuts for the frontmost app. Uses hardcoded structure for the shortcuts",
      "mode": "view"
    },
    {
      "name": "current-app",
      "title": "Copy current app's bundle id",
      "description": "Stores current app's bundle id in the clipboard as a concealed entry",
      "mode": "no-view"
    },
    {
      "name": "current-shortcuts",
      "title": "Show shortcuts for the frontmost app",
      "description": "Taked current bundle id and launches other command",
      "mode": "no-view"
    },
    {
      "name": "app-shortcuts",
      "title": "Show shortcuts for app that passed in context",
      "description": "Accepts bundle id as param",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.60.1",
    "@raycast/utils": "^1.10.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "18.18.4",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
